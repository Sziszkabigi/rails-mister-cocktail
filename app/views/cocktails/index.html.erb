<div class="container">


  <h2>All Cocktails</h1>
     <ul >
       <% @cocktails.each do |cocktail| %>
        <li>
          <%= link_to cocktail.name, cocktail_path(cocktail) %>
        </li>
       <% end %>
    </ul>
 <%= link_to "Homepage", root_path, class: 'btn btn-success' %>

</div>
